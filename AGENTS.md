# Codex Project Instructions

- Use the `apply_patch` file-editing tool for all manual source changes so every edit is exposed in the Codex UI for review and rollback.
- Do not edit source files through shell redirection, temporary patch files, or ad hoc scripts unless `apply_patch` cannot perform the required mechanical change.
